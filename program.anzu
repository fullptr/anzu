// Project Euler #1 No OR operator, index stored in stack
0 -> count
input -> max

0 while dup max < do
    if dup 3 % 0 == do
        dup count + -> count
    else
        if dup 5 % 0 == do
            dup count + -> count
        end 
    end
    1 +
end

count .