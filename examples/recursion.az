# fibbonacci to demonstrate functions and recursion

function fibb(n: int) -> int do
    if n == 0 {
        return 0
    } else if n == 1 {
        return 1
    }

    return fibb(n - 1) + fibb(n - 2)
end

i = 0
while i < 10 do
    println(fibb(i))
    i = i + 1
end