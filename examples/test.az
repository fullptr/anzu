

struct pair
{
    x: i64;
    y: i64;

    fn sum(self: &pair) -> i64
    {
        return self->x + self->y;
    }
}

struct wrapper
{
    z: &pair;
};

p := pair(1, 2);
w := wrapper(&p);

println(w.z->sum());