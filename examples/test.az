

struct vec2
{
    x: f64;
    y: f64;

    fn length2(self: &vec2) -> f64
    {
        return (self->x * self->x) + (self->y * self->y);
    }
}

v := vec2(3.0, 4.0);
pv := &v;
println((*pv).length2());