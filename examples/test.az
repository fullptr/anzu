let std := @import("lib/std.az");

fn qux!(T)(x: T) -> T
{
    return T();
}

fn foo!(T)(x: T) -> T
{
    return qux(x);
}

let x := [1, 2, 3, 4];

#for e in std.enumerate(std.pairwise(x[])) {
#    print("{}: {} {}\n", e.index, e.value.left@, e.value.right@);
#}

print("{}\n", foo(10));