
l := [234345, 543656742, 234234123434, 45623234];

struct span
{
    ptr: &i64;
    size: u64;

    fn at(self: &span, idx: u64) -> i64
    {
        return *(self->ptr + idx);
    }
}

s := span(&l[0u], size_of(l) / size_of(l[0u]));