module m := "examples/module.az";
module i := "lib/io.az";

struct double {
    x: m.pair;
    y: m.pair;
}

let d := double(m.pair(1, 2), m.pair(3, 4));
print("{} {} {} {}\n", d.x.x, d.x.y, d.y.x, d.y.y);