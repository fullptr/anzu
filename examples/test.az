
function foo(a: int, b: int) -> int do
    
    function nested(c: int) -> int do
        return c
    end

    x = nested(a + b)
    println(typeof(x))

    return nested(a + b)
end

x = foo(2, 3)

println(x)