struct pair!(T)
{
    x: T;
    y: T;

    fn print_struct(self: pair!(T) const&) -> null
    {
        print("{} {}\n", self.x, self.y);
    }
}

{
    var f := pair!(i64)(1, 2);
    var g := pair!(f64)(12.3, 45.6);
    f.print_struct();
    g.print_struct();
}