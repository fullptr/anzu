
struct unique_int
{
    val: i64;

    fn copy = delete;
    fn assign = delete;
    fn drop(self: &unique_int)
    {
        println("dropping");
        println(self->val);
    }
}

fn add(x: i64, y: i64) -> i64
{
    return x + y;
}

fn myfunc(x: unique_int)
{
    println(x.val);
}

{
    x := [1, 2, 3];
    x = [4, 5, 6, 7];
    println(x[0u]);
}