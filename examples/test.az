
struct unique_int
{
    val: i64;

    fn copy = delete;
    fn assign = delete;
    fn drop(self: &unique_int)
    {
        println("dropping");
    }
}

fn add(x: i64, y: i64) -> i64
{
    return x + y;
}

fn myfunc(x: unique_int)
{
    println(x.val);
}

{
    t := new i64;
    x := [t, t];
    y := x;
    println(y[0u]);
    delete t;
}