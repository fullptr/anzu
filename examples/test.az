

module vec := "lib/tokenizer.az";

let input := "my test string has six words";
var tok := tokenizer.create(input, ' ');
while tok.valid() {
    print("{}\n", tok.current());
    tok.advance();
}

print("{}\n", global);