

struct pair!(T)
{
    x: T;
    y: T;

    fn print_struct!(U)(self: const&, u: U) -> null
    {
        print("{} {} {}\n", self.x, self.y, u);
    }
}

var f := pair!(i64)(1, 2);
var g := pair!(f64)(12.3, 45.6);
f.print_struct!(u64)(70u);
__dump_type(f, g, f.x, g.x);