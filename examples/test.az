

module vec := "std/vector.az";

arena a;

var v := vector!(i64).create(a&);
var idx := 0;
while idx < 10 {
    v.push(idx);
    idx = idx + 1;
}
print("here size={} capacity={}\n", v.size(), v.capacity());