


struct vec2
{
    x: float
    y: float

    fn length(self: &vec2) -> float
    {
        return sqrt(self->x * self->x + self->y * self->y)
    }
}

v := vec2(3.0, 4.0)
println(v.length())

p := &v

println(p->x)