

fn get(a: arena&) -> i64&
{
    var p := a@.create<i64>(5);
    p@ = 5;
    print("addr={} value={}, value_at={}\n", p&, p, p@);
    return p;
}

arena a;
var z := get(a&);
var y := a.create<i64>(10);
var x := a.create<i64>(6);

print("addr={} value={}, value_at={}\n", z&, z, z@);
print("addr={} value={}, value_at={}\n", y&, y, y@);
print("addr={} value={}, value_at={}\n", x&, x, x@);