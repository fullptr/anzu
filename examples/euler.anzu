# project euler #1, sum of multiple of 3 and 5 below 1000

0 -> count

0 while dup 1000 < do
    if dup 3 % 0 == do
        dup count + -> count
    elif dup 5 % 0 == do
        dup count + -> count
    end
    1 + 
end

count .