# project euler #1, sum of multiple of 3 and 5 below 1000

x := 5u;

y := new i64 : x;

*(y + 2u) = 5;
*(y + 3u) = 6;

idx := 0u;
while idx < x {
    println(*(y + idx));
    println(y + idx);
    idx = idx + 1u;
}

